const validateBook = require('./validateBook');
const error = require('./error');
const validateBookUpdate = require('./validateBookUpdate');

module.exports = {
  validateBook,
  error,
  validateBookUpdate,
};
